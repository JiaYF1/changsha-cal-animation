# 长沙机场土石方计算动画设计说明

## 项目与目标

本项目依据《长沙机场改扩建工程地基处理和土方工程三维地质建模及土石方计算报告》，面向业主及非三维地质建模专业人员，制作一个可直接在浏览器中打开的单页面动画。目标不是展示 GOCAD 的菜单操作，而是让观众在较短时间内理解计算为什么成立、各类输入数据分别发挥什么作用、土方与石方如何从同一个开挖体中被区分，以及最终方量如何按区域汇总。动画应能自动播放，也能暂停、前后分步和拖动进度，以便汇报人停在关键画面进行解释。

## 核心叙事

动画围绕一个持续存在的三维机场场区模型展开，不采用互不相干的幻灯片切换。第一段从 CAD 提供的原始地面高程点、基岩顶部高程点、设计底标高和计算范围开始，强调三类高程数据与一个边界共同构成计算输入。第二段将离散点逐步连接并光滑成连续曲面，用简化但准确的方式表达 DSI：已知点提供约束，插值在满足约束的同时尽量降低曲面粗糙度。第三段把原始地表与设计开挖面上下对照，并沿计算范围封边，形成封闭开挖包络体；应通过透明地层、剖切或爆炸分层让观众清楚看见“原地面以下、设计面以上”的体积就是总挖方。第四段引入基岩顶面作为土岩分界，使同一开挖体在空间上连续分离为上部土体和下部岩体，避免使用仅有色块和箭头的抽象示意。第五段将模型按一标段、二标段和航站楼区域高亮拆分，方量数字从对应实体中汇聚到结果栏，最后给出总挖方 22,249,200 m3，其中土方 11,138,600 m3、石方 11,110,600 m3。

## 真实内容与数据

输入必须显示：设计底标高、原始地面高程、基岩顶部高程、计算范围。区域结果必须使用报告数据：一标段总挖方 8,277,400 m3，土方 4,179,010 m3，石方 4,098,390 m3；二标段（不含航站楼）总挖方 12,677,317 m3，土方 6,377,832 m3，石方 6,299,485 m3；航站楼总挖方 1,294,483 m3，土方 581,758 m3，石方 712,725 m3。动画可在最终结果处补充土石占比，但不编造精度、误差或计算耗时。

## 观众、语气与视觉原则

观众重点关心计算边界是否明确、土岩分界是否有依据、方量是否可追溯，而不是算法公式本身。整体语气应专业、克制、可信，避免游戏化粒子爆炸、通用科技蓝紫渐变和大量装饰性卡片。配色建议以地表植被灰绿、土体赭红、岩体冷灰、设计面工程橙作为语义色，所有颜色在三个方向中保持含义一致。文字只承担命名和结论，原理主要通过空间关系、剖切、透明度和连续运动说明。报告内嵌模型截图作为真实项目证据使用，三维原理模型可用 Three.js 重建为更清楚的可交互示意；两者需在视觉上明确区分“项目数据画面”和“原理解释画面”。

## 输出与交互约束

方向初稿为一个 1920x1080 比例的单页面对比视图，包含三个差异化方向板，每个方向板呈现一个真实关键帧、色板与一句定位。用户选定后，深化为单页面完整动画，适配桌面投影和 16:9 屏幕，同时在窄屏上不发生文字或控制条重叠。完整动画预计 90 至 150 秒，保留自动播放、播放/暂停、上一步、下一步、进度拖动、章节跳转、重播和视角轻微旋转控制；动画需尊重减少动态效果设置。固定画面不使用视口字体缩放，三维舞台使用稳定宽高比，布局变化不得导致控制条跳动。

## 三个方向的差异维度

方向 A 以“透明地层剖切”为核心，像工程剖面模型一样逐层揭示，适合强调可信和易讲解。方向 B 以“数字孪生扫描”为核心，在真实机场模型图与简化三维场景之间建立对应，适合强调项目规模和技术能力。方向 C 以“方量守恒实验台”为核心，用一镜到底的实体分离和计量过程展示总量如何拆成土、岩及各区域，适合强调计算逻辑与结果可核对。三版必须使用同一组真实数据与语义色，但构图、镜头和主视觉动作不同。

